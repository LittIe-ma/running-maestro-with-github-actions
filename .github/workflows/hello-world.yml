name: Hello and Goodbye Workflow

# 1. Event
on:
  workflow_dispatch: # 手動実行

# 2. Workflow
jobs:
  hello_world: # job_1
    runs-on: macos-latest

    # 👉 steps for job_1
    steps:
      - name: Hello World!
        run: echo "hello world."

      - name: Today's Date!
        run: date
